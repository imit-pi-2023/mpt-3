{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.5.2",
    "prettier": "^2.7.1"
  },
  "scripts": {
    "test": "lerna run test",
    "prettier": "prettier --check packages",
    "prettier:write": "prettier --write packages"
  },
  "dependencies": {
    "@wdio/cli": "^8.16.15"
  }
}
